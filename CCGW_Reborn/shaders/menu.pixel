#version 420 core

in vec2 texOut;
out vec4 finalColor;

uniform sampler2D texSampler;

void main (){
	finalColor = texture(texSampler, texOut);
	/*if (color.w == 0)
		finalColor = vec4(0);	
	else
		finalColor = color;*/
}
