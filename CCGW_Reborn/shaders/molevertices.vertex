#version 420 core

in vec3 pos ;
in vec3 normal ;
in vec2 texPos ;
in vec3 tangent ;
in vec3 biTangent ;

out vec4 posOut;
out vec3 normalOut;
out vec2 texPosOut;
//out vec3 tangentOut;
//out vec3 biTangentOut;

uniform mat4 world;

void main(){
	posOut = vec4(pos.xy, -0.6, 1.0);
	normalOut = normal;
	texPosOut = texPos;
	//tangentOut = tangent;
	//biTangentOut = biTangent
}